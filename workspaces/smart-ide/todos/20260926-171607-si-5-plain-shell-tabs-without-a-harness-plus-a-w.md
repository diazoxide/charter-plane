# SI-5 Plain shell tabs without a harness, plus a warning when a harness i

_2026-09-26 17:16 · persistent_

SI-5 Plain shell tabs without a harness, plus a warning when a harness is launched by hand inside one
