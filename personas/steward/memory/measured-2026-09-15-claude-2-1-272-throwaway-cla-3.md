# Measured 2026-09-15, claude 2.1.272, throwaway CLAUDE_CONFIG_DIR: Claude

_2026-09-15 20:13 · persistent_

Measured 2026-09-15, claude 2.1.272, throwaway CLAUDE_CONFIG_DIR: Claude Code validates the WHOLE plugins/installed_plugins.json before loading any plugin. version must be 1 or 2 (missing/3 rejected); v2 plugins must be an object of arrays; every record under ANY plugin id must be an object with scope in managed/user/project/local, installPath a string, projectPath absent or a string (null or number rejected). One bad record anywhere, first or last, logs 'Failed to load installed_plugins.json' and loads NO plugin. A version-1 list is rewritten at start-up to user-scope records and loads. Missing, empty or NUL projectPath is valid and means not this directory (binary e1: user/managed reach; projectPath===cwd; !projectPath -> no; same repo root). A reaching record whose installPath dir is gone loads nothing unless another reaching record has its files. Missing plugin files are restored by 'claude plugin list --json' (so charter doctor --fix restores them), not by a new session, and 'claude plugin install' only says already installed. CLAUDE_CODE_PLUGIN_CACHE_DIR set non-empty moves the install list (list shows 0 installs); set empty it is ignored.
