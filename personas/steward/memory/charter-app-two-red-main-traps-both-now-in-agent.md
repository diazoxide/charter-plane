# charter-app: two red-main traps, both now in AGENTS.md (PR #140). (1) In

_2026-09-22 02:14 · persistent_

charter-app: two red-main traps, both now in AGENTS.md (PR #140). (1) In a React test, a PRECONDITION must wait with Testing Library's waitFor, not vi.waitFor — only RTL's polls inside act. PlaneView sets settled and calls chatsThatWouldNotStart in the same block, so the IPC is recorded synchronously while the state is merely queued, and App reads settled through a ref a useLayoutEffect fills. vi.waitFor returns in that gap about 1 run in 6, the quit then hits an unsettled window and warns instead of quitting. vi.waitFor stays fine for CLOSING assertions about things outside React. (2) A scenario spec must ask for a control by role, never by tag: input[type=radio] became button[role=radio] under Radix and three copies of that selector each burned a 20 s timeout. e2e/opening.ts now owns HARNESS_ROWS = '[aria-labelledby="pick-harness"] [role="radio"]'. ALSO, process: two PRs merged ~20 s apart both went green because GitHub tests each PR's own merge commit — neither run saw the other, and main was red on three jobs. Overlapping PRs that touch shared selectors need the second one re-run against the merged main before merging.
