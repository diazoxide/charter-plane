# charter-app#129: the repo is checked out INSIDE the operator's plane (wo

_2026-09-21 10:25 · persistent_

charter-app#129: the repo is checked out INSIDE the operator's plane (workspaces/ide/charter), so plane::find_root walking up from any test process answers with the operator's real plane — a scenario/bench app run with no $CHARTER_ROOT read and rewrote /Users/aharon/IdeaProjects/charter/.charter/app/reopen.json, which is an execution input (ADR 0035). Fix (PR #132) is a compile-time fence in charter-core: a 'fenced' cargo feature turned on ONLY from [dev-dependencies] (cargo unifies features across a test build's unit graph, so the binary an integration test spawns is fenced too — verified) and from the app's e2e feature; charter_core::fence::hold panics if a resolved/opened/read/written plane, or the machine store, is outside $CHARTER_PLANE_FENCE (default: the system temp dir). Held at plane::walk (the one line find_root and place share), resolve's CHARTER_ROOT arm, reopen::write/read_or_refusal, machine::config_root and Planes::open.
