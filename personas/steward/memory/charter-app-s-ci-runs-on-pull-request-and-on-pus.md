# charter-app's CI runs on pull_request and on pushes to main only, so a m

_2026-09-20 10:06 · persistent_

charter-app's CI runs on pull_request and on pushes to main only, so a mutation probe branch reports nothing until a (draft) PR is opened for it. Open one, read the failures, then close it and delete the branch.
