# charter-app's palette is 117 rows at fifty chats, and the burial is TABS

_2026-09-20 21:30 · persistent_

charter-app's palette is 117 rows at fifty chats, and the burial is TABS not workspaces (charter-app#48, 2026-09-20). Measured with catalogue() as a pure function in vitest — no window, no stress job needed. Typing 're' used to list 'Remove this chat's worktree' 41st of 41 matches, behind release.3/reddit.7/user-reporting.8 and their close-tab twins: 19 tab rows + 19 close-tab rows against 3 workspace rows. So the tmux frame's remedy (keep workspaces out of the browsable list) would not have moved the number — ranking beats filtering here, and the measurement is what decided it. Fix: Offer carries the name its title interpolates, and narrow() puts words-matches before name-only matches (stable two-group partition, no score). Second find the same measurement turned up: .palette-rows scrolls at 55vh and nothing ever scrolled the aim into view.
