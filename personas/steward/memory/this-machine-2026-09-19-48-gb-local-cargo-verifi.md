# This machine (2026-09-19, 48 GB): local cargo verification of charter-ap

_2026-09-19 02:30 · persistent_

This machine (2026-09-19, 48 GB): local cargo verification of charter-app is UNRELIABLE under fleet load, and the failure does not look like a build error. Measured while ~96 claude processes were running: free memory ~0.9 GB of 48 GB, swap 4.67 GB used of 6 GB, load 7.8 — and 'cargo test -p charter-app --no-run' had rustc killed by SIGNAL 9 (SIGKILL) partway through the Tauri dependency tree, compiling 'ctor'. That is macOS jetsam reclaiming the biggest memory consumer, not a compile failure, and the error text says only 'could not compile ctor (lib) ... (signal: 9, SIGKILL: kill)'. charter-core is unaffected: it builds and tests in ~16 s and survived 21 back-to-back mutation cycles. So: a charter-app branch verified only locally on this machine is not verified; say CI-only. Before blaming code for a killed or hung cargo run here, check 'vm_stat', 'sysctl vm.swapusage' and how many claude processes are up. Retry with '-j 2' when the fleet is quiet rather than piling more load on a thrashing machine, and NEVER pkill by name to free memory — other chats run as claude.
